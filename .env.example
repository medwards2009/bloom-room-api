# Server
PORT=8080

# Database (matches docker-compose.yaml)
DB_HOST=localhost
DB_PORT=5432
DB_USER=bloom
DB_PASSWORD=secret
DB_NAME=bloom_room_dev

# Auth — API-issued JWT
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Google Sign-In: comma-separated list of allowed OAuth client IDs (mobile/web)
GOOGLE_CLIENT_IDS=
